CORS: CORS means cross-origin resouce sharing, its a mechanism that uses additional HTTP headers to tell browsers to give a web app is running at one orgin. acess to selected resources from different orgin.but main point is both the web app should have different origin, if they have same origin they can share resources very easily. but if they dont have same origin they needs to follow corss mechanism.
Authorization:The http Authorization request header contains the credentials to authenticate a user agent with a server, usually, but not necessarily, after the server has responded with a 401 Unauthorized status.